// Database
const DB_HOST = 'localhost'
const DB_USER = 'root'
const DB_PASSWORD = '0937542717Ss'
const DB_DATABASE = 'simulation'
const DB_PORT = 3306

// Server
const SERVER_PORT = 3000


module.exports = {
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_DATABASE,
  DB_PORT,
  SERVER_PORT,
}